syntax = "proto3";
package services;
option go_package = "./;proto";


service Hello {
  // 定义SayHello方法
  rpc SayHello (HelloRequest) returns (HelloReply);
}

message HelloRequest {
  string name = 1; //编号
}

message HelloReply {
  string message = 1; //编号
}